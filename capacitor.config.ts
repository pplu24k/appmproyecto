import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appm_proyecto',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
